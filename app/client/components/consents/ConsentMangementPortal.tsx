import { css } from "@emotion/core";
import React from "react";

const headerStyles = css`
  color: red;
`;

export const ConsentManagementPortal = () => (
  <h1 css={headerStyles}>Hello World!</h1>
);
