import * as React from "react";
import { RouteableProps, WizardStep } from "../../wizardRouterAdapter";

export const SaveOfReasonC = (props: RouteableProps) => (
  <WizardStep routeableProps={props}>
    <h1>Save of Reason C</h1>
  </WizardStep>
);
